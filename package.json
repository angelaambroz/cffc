{
  "name": "app",
  "version": "1.0.0",
  "description": "``` # One-liner to get a clean Node environment docker run -it --rm -v $(pwd):/app -w /app -p 8080:8080 node:20-slim bash",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "node-fetch": "^3.3.2"
  }
}
